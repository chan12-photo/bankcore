# Default local development uses the Vite proxy, so this can stay blank.
VITE_BANKCORE_API_BASE_URL=

# Change this only if the Spring Boot backend is not running on localhost:8080.
VITE_BANKCORE_PROXY_TARGET=http://localhost:8080
